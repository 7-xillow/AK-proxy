version: '3'

services:
  xillow_summary:
    build: submodule/AK-service/
    depends_on:
      - 'database'
    ports: 
      - '3002:3002'

  database:
    image: mongo:latest
    ports:
      - '27017:27017'
